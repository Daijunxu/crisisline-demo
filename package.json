{
  "name": "crisisline-demo",
  "version": "1.0.0",
  "description": "EmpathZ AI Coordinator - Crisis Hotline Call Analysis",
  "scripts": {
    "dev": "cd frontend && python3 -m http.server 3000",
    "build": "echo 'Frontend is already built (single HTML file)'",
    "deploy": "vercel --prod",
    "preview": "vercel"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}